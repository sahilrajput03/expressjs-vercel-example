{
	"version": 2,
	"builds": [
		{
			"src": "./dist/app.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	],
	"outputDirectory": "dist",
	"buildCommand": "npm run build",
	"devCommand": "node ./dist/app.js"
}